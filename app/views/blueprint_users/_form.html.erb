<%= form_for(@blueprint_user) do |f| %>
  <% if @blueprint_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blueprint_user.errors.count, "error") %> prohibited this blueprint_user from being saved:</h2>

      <ul>
      <% @blueprint_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :blueprint_id %><br>
    <%= f.number_field :blueprint_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :preferred_decryptor %><br>
    <%= f.number_field :preferred_decryptor %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
